"""Seed-data definitions. Imported by demo/seed_demo.py at seed time.

Each module exports a top-level constant (SKILLS, CUSTOMER_VOICE, ...) that's
a list of dicts ready for mongo_tools.insert_many(). Keep definitions here
rather than inlined in seed_demo so the data layer is reviewable separately
from the orchestration.
"""
